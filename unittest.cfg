[unittest]
plugins=nose2.plugins.junitxml

code-directories=xos-genx
                 xos-api
                 xos-config
                 xos-synchronizer
                 coreapi

[coverage]
always-on = True
coverage = xos
           lib
coverage-report = term
coverage-report = xml
