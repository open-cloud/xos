[unittest]
plugins=nose2.plugins.junitxml

code-directories=xos-genx
                 xos-config
                 coreapi

[coverage]
always-on = True
coverage = xos
           lib
coverage-report = term
coverage-report = xml
